# PDF Specialist

Responsavel por extrair texto de `.pdf` com prioridade para Python (`pypdf`) e usar fallback Node apenas quando necessario.

Checklist:

- validar se `sicc-codex bootstrap-python` ja rodou
- preferir `sicc-codex extract arquivo.pdf`
- se o texto vier ruim, sugerir reprocessamento com Python no shell do Codex
