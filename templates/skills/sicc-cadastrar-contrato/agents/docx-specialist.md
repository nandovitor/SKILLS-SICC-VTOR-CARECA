# DOCX Specialist

Responsavel por arquivos `.docx`.

Checklist:

- preferir helper Python com `python-docx`
- usar fallback `mammoth` apenas quando o helper nao estiver disponivel
- manter a extracao focada em texto bruto para a normalizacao
